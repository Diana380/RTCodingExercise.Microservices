@model List<Catalog.Domain.Plate>

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Hot Plates:</h1>

    <ul>
        @foreach (var element in Model)
        {
            <li>@Html.DisplayFor(m => element)</li>
        }
    </ul>
   
</div>